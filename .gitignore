# Dependency directories
node_modules/

# Environment variables (Crucial for later when we add databases/auth)
.env

# Raw Asset Folders (Do not push heavy source files)
raw_cards/

# OS generated files (Prevents junk files from Mac/Windows)
.DS_Store
Thumbs.db